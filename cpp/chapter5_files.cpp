#include <iostream>

using namespace std;
int main() {
  /*Before data is written to or read from a file, 2 things must happen:*/
  // 1. A file stream object must be created.
  // 2. The file must be opened and linked to the file stream object

  /*Example:
    ifstream inputFile;
    inputFile.open("Customers.txt");
    */
}
